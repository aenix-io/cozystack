apiVersion: cozystack.io/v1alpha1
kind: Workload
metadata:
  labels:
    app.kubernetes.io/name: cozystack-workload-controller
    app.kubernetes.io/managed-by: kustomize
  name: workload-sample
spec:
  # TODO(user): Add fields here
